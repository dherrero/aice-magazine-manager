import Magazine, { MagazineModel } from './magazine.model';
import Page, { PageModel } from './page.model';
import User, { UserModel } from './user.model';

export { Magazine, MagazineModel, Page, PageModel, User, UserModel };
