{
  "name": "code-reader",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/code-reader/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "code-reader:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "code-reader:build:development"
        },
        "production": {
          "buildTarget": "code-reader:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/code-reader/jest.config.ts"
      }
    }
  }
}
